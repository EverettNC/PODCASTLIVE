node_modules/
.project_id
.github_repo
.env
.env.*

# Third Seat runtime outputs: renders and the append-only show log are not source
show/renders/
show/*.jsonl

# API keys dropped in the Build bay live here, owner-only, never in the repo
show/keys.env
